import React from 'react';
import {Text, View} from 'react-native';

const info = ({navigation}) => {
  return (
    <View style={{flex: 1}}>
      <Text>info</Text>
    </View>
  );
};

export default info;
